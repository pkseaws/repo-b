name: Deploy Java
description: Deploy Java app
inputs:
  env:
    description: "Envirment required"
    required: true

runs:
  using: "composite"
  steps:
    - run: echo "Deploying Java app to ${{ inputs.env }}"
      shell: bash
      echo "Deploying ${{ inputs.language }} app to ${{ inputs.env }}"
     
